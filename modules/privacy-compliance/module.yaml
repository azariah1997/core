name: privacy-compliance
owner: platform
lifecycle: experimental
dependencies:
  - users
# Note: audit (backend/core-api/internal/audit) lives inside this module
# too (see README) - it was never split into its own top-level entry in
# modules/README.md's 16-domain list, so it's not a separate dependency.
